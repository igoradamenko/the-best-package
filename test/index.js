const assert = require('assert');

const tbp = require('..');

describe('Main suite', () => {
  it('should equal 0', () => {
    // TODO: fix it when we implement the package
    assert.deepStrictEqual(tbp, 0);
  });
});
